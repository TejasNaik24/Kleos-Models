# ---------------------------------------------------------------------------
# Runtime dependencies for the Hermes v0.0.6 inference container.
#
# torch is NOT listed here. It is installed first, in its own layer, from the
# PyTorch CUDA 12.8 index (see docker/hermes.Dockerfile), so this file can be
# resolved against PyPI without dragging in PyPI's default CUDA build. pip
# treats the installed 2.11.0+cu128 as satisfying the `torch==2.11.0` that
# peft, accelerate and bitsandbytes ask for, so it is never replaced.
#
# MODEL RUNTIME — exact versions recorded in the kleos-v006-mistralnemo12b-run1
# manifest (transformers 5.16.1, peft 0.20.0, accelerate 1.14.0,
# bitsandbytes 0.50.2, torch 2.11.0+cu128). These decide tokenization,
# quantized arithmetic and generation, i.e. whether the served model is the
# model that was measured. Do not float them.
#
# The 9/9 exact-match smoke test ran in a Colab session whose full package set
# was not recorded. After the first start on a GPU host, re-run
# scripts/hermes_smoke.py inside this container to re-establish the match
# under exactly these versions. See docs/deployment.md.
# ---------------------------------------------------------------------------

# --- model runtime (pinned to the research record) --------------------------
transformers==5.16.1
peft==0.20.0
accelerate==1.14.0
bitsandbytes==0.50.2

# --- serving layer (the versions the API tests pass against) ---------------
fastapi==0.141.1
uvicorn==0.53.0
anyio==4.15.1

# --- kleos-models core dependencies -----------------------------------------
pydantic==2.13.5
PyYAML==6.0.3
jsonschema==4.26.0
numpy==2.5.2

# --- everything else, pinned from the resolver ---------------------------------
# Generated by resolving this file plus torch==2.11.0 for linux/x86_64,
# CPython 3.12 on 2026-09-23 — not hand-written. Pinned so a rebuild installs
# the same packages. Two of these change outputs directly, so they are not
# incidental: tokenizers performs the pre-tokenization, and Jinja2 renders the
# chat template into the prompt.
#
# Resolution caveat: pip evaluates environment markers against the machine it
# runs on, so Linux-only dependencies (torch's nvidia-* and triton packages)
# are absent here. They come from the CUDA 12.8 index in torch's own layer, and
# the image records the full installed set in /app/requirements.lock.
Jinja2==3.1.6
MarkupSafe==3.0.3
Pygments==2.21.0
annotated-doc==0.0.5
annotated-types==0.8.0
attrs==26.1.0
certifi==2026.7.22
click==8.5.0
filelock==4.0.1
fsspec==2026.9.0
h11==0.16.0
hf-xet==1.6.0
httpcore==1.0.9
httpx==0.28.1
huggingface_hub==1.32.0
idna==3.20
jsonschema-specifications==2025.9.1
markdown-it-py==4.2.0
mdurl==0.1.2
mpmath==1.3.0
networkx==3.7
packaging==26.3
psutil==7.2.2
pydantic_core==2.46.5
referencing==0.37.0
regex==2026.9.10
rich==15.0.0
rpds-py==2026.6.3
safetensors==0.8.0
setuptools==81.0.0
shellingham==1.5.4
starlette==1.6.0
sympy==1.14.0
tokenizers==0.23.2
tqdm==4.70.1
typer==0.27.2
typing-inspection==0.4.4
typing_extensions==4.16.0
