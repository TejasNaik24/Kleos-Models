# Core (data/config/evaluation-scoring layers — no torch required):
pydantic>=2.7,<3
PyYAML>=6.0
jsonschema>=4.21
numpy>=1.26

# Model loading + training (Colab / CUDA machines):
torch>=2.4
transformers>=4.56,<6
peft>=0.12
accelerate>=0.34
datasets>=2.20
safetensors>=0.4.3
huggingface-hub>=0.25
sentencepiece>=0.2

# Quantized training (CUDA only):
bitsandbytes>=0.43

# Tests, linting, type checking, notebook building:
pytest>=8.0
pytest-cov>=5.0
ruff>=0.6
mypy>=1.11
types-PyYAML
nbformat>=5.10
